{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_input.notebook_path // empty' | { read -r f; case \"$f\" in */frontend/*) fe=\"${f%%/frontend/*}/frontend\"; (cd \"$fe\" && npx --no-install prettier --write \"$f\");; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
